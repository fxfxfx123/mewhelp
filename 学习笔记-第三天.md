# MewHelp 学习笔记 —— 第三天：ReAct 循环的执行机制

> 日期：2026-09-14
> 仓库：https://github.com/fxfxfx123/mewhelp
> 目标：理解 `main_agent` ⇄ `agent_tools` 循环怎么运转，能画出完整的循环过程

---

## 一、ReAct 循环是什么？

### 真实场景

用户说："我的快递到哪了？"

**系统内部发生的事**：

```
① 用户问题进来
② 模型思考："需要查物流，我要调用 query_logistics 工具"
③ 系统执行工具，拿到结果："您的快递在广州，预计明天送达"
④ 模型拿到结果，生成回复："根据物流信息，您的快递在广州，预计明天送达"
⑤ 返回给用户
```

**这个过程就是 ReAct 循环**：**Re**asoning（推理）+ **Act**ing（行动）交替进行。

---

## 二、循环的骨架：两个节点 + 一个判断

### 代码结构（build.py:47-52）

```python
# 主力 Agent ReAct 环
b.add_conditional_edges("main_agent", should_continue, {
    "continue": "agent_tools",
    "stop": "log",
})
b.add_edge("agent_tools", "main_agent")
```

### 流程图

```
    ┌─────────────┐
    │ main_agent  │ (调模型，决定要不要调工具)
    └──────┬──────┘
           │
      should_continue? (判断：有 tool_calls 吗？)
         ／  ＼
      continue  stop
       ／          ＼
      ↓            ↓
┌────────────┐   ┌─────┐
│agent_tools │   │ log │ → END
└─────┬──────┘   └─────┘
      │
      └──回到 main_agent (循环)
```

**关键点**：
- `main_agent` → `should_continue` → `agent_tools` → `main_agent` 形成**循环**
- `should_continue` 是**出口**：没有 tool_calls 就跳到 `log` 结束

---

## 三、完整示范：一次 ReAct 循环（参数缺失场景）

### 场景：用户说"我的快递到哪了？"

假设已经过了 `classify_intent`，判定为"物流"，进入 `business` 路由 → `main_agent`。

### 初始 state

```python
{
    "messages": [HumanMessage("我的快递到哪了？")],
    "conversation_id": 42,
    "user_id": "u123",
    "intent": "物流",
    "route": "business",
    "steps": 0,  # ← 循环计数器
    "tokens_used": 0
}
```

---

### 第 1 步：main_agent（第一次调用）

**节点做什么**（nodes.py:315-334）：

```python
async def main_agent(state, config=None) -> dict:
    specs = await registry.get_all_specs()   # 获取所有工具定义（内置+MCP）
    model = get_chat_model(streaming=True).bind_tools([s.tool for s in specs])
    msgs = _agent_messages(state)  # 拼装消息列表
    ai: AIMessage = await model.ainvoke(msgs, config)  # 调用模型
    return {
        "messages": [ai],
        "steps": state.get("steps", 0) + 1,  # steps 加 1
        "tokens_used": state.get("tokens_used", 0) + used
    }
```

**发给模型的内容**（简化版）：

```
SystemMessage: "你是客服助手，有以下工具可用：query_logistics, query_order, ..."
HumanMessage: "我的快递到哪了？"
```

**模型返回**（AIMessage 带 tool_calls）：

```python
AIMessage(
    content="",  # ← 没有文本回复
    tool_calls=[
        {
            "id": "call_abc123",
            "name": "query_logistics",
            "args": {"order_id": ""}  # ← 模型发现缺订单号
        }
    ]
)
```

**state 变化**：

```python
{
    "messages": [
        HumanMessage("我的快递到哪了？"),
        AIMessage(content="", tool_calls=[...])  # ← 追加
    ],
    "steps": 1,  # ← 0 → 1
    "tokens_used": 150,
    ...
}
```

---

### 第 2 步：should_continue（第一次判断）

**判断逻辑**（routing.py:34-48）：

```python
def should_continue(state) -> str:
    last = state["messages"][-1]  # 拿最后一条消息
    has_tool_calls = isinstance(last, AIMessage) and bool(last.tool_calls)
    if not has_tool_calls:
        return "stop"
    if state.get("steps", 0) >= settings.max_agent_steps:
        return "stop"
    return "continue"
```

**执行**：
- `last = AIMessage(tool_calls=[...])`
- `has_tool_calls = True` ✅
- `steps = 1 < max_agent_steps` ✅
- 返回 `"continue"`

**结果**：跳转到 `agent_tools` 节点

---

### 第 3 步：agent_tools（执行工具）

**节点做什么**（nodes.py:337-407）：

```python
async def agent_tools(state) -> dict:
    last = state["messages"][-1]
    specs = {s.name: s for s in await registry.get_all_specs()}
    
    tool_msgs = []
    for tc in last.tool_calls:  # 遍历所有工具调用
        # 统一执行引擎
        run = await engine.execute_tool_call(tc, cid, specs, user_id=uid)
        tool_msgs.append(run.tool_message)
    
    return {"messages": tool_msgs}
```

**执行过程**：
1. 拿到 `tool_calls = [{"name": "query_logistics", "args": {"order_id": ""}}]`
2. 调用 `engine.execute_tool_call()`
3. 工具发现 `order_id` 为空 → 返回错误消息

**工具返回**（ToolMessage）：

```python
ToolMessage(
    content="参数校验失败：order_id 是必需的",
    tool_call_id="call_abc123",
    name="query_logistics",
    status="error"
)
```

**state 变化**：

```python
{
    "messages": [
        HumanMessage("我的快递到哪了？"),
        AIMessage(tool_calls=[...]),
        ToolMessage(content="参数校验失败：order_id 是必需的")  # ← 追加
    ],
    "steps": 1,
    ...
}
```

**下一步**：`agent_tools` 有一条固定边 → 回到 `main_agent`（循环！）

---

### 第 4 步：main_agent（第二次调用）

**发给模型的内容**：

```
SystemMessage: "你是客服助手..."
HumanMessage: "我的快递到哪了？"
AIMessage: tool_calls=[query_logistics(order_id="")]
ToolMessage: "参数校验失败：order_id 是必需的"  # ← 模型看到错误
```

**模型返回**：

```python
AIMessage(
    content="请问您的订单号是多少呢？",
    tool_calls=[]  # ← 没有工具调用了
)
```

**state 变化**：

```python
{
    "messages": [
        HumanMessage("我的快递到哪了？"),
        AIMessage(tool_calls=[...]),
        ToolMessage("参数校验失败..."),
        AIMessage(content="请问您的订单号是多少呢？", tool_calls=[])  # ← 追加
    ],
    "steps": 2,  # ← 1 → 2
    "tokens_used": 300,
    ...
}
```

---

### 第 5 步：should_continue（第二次判断）

**执行**：
- `last = AIMessage(content="请问您的订单号...", tool_calls=[])`
- `has_tool_calls = False` ❌
- 返回 `"stop"`

**结果**：跳转到 `log` 节点 → END

---

### 完整循环流程图

```
main_agent (步骤1, steps=0→1)
   ↓ 返回：AIMessage(tool_calls=[query_logistics])
should_continue
   ↓ 判断：有 tool_calls → "continue"
agent_tools (执行工具)
   ↓ 返回：ToolMessage("参数校验失败")
main_agent (步骤2, steps=1→2)
   ↓ 返回：AIMessage(content="请问订单号", tool_calls=[])
should_continue
   ↓ 判断：无 tool_calls → "stop"
log → END
```

---

## 四、关键机制解析

### 机制 1：`steps` 字段防止死循环

**每次进入 `main_agent`，`steps` 就加 1**：

```python
return {
    "steps": state.get("steps", 0) + 1,
    ...
}
```

**为什么需要 `steps`？**

防止**死循环**！看 `should_continue` 的第二个判断：

```python
if state.get("steps", 0) >= settings.max_agent_steps:
    return "stop"
```

**场景**：模型一直调用同一个工具，工具一直返回错误，模型又继续调用...

```
main_agent → query_logistics → 错误
main_agent → query_logistics → 错误
main_agent → query_logistics → 错误
... (无限循环)
```

**`steps` 封顶**：假设 `max_agent_steps = 5`

```
步骤1: steps=1, 有 tool_calls → continue
步骤2: steps=2, 有 tool_calls → continue
步骤3: steps=3, 有 tool_calls → continue
步骤4: steps=4, 有 tool_calls → continue
步骤5: steps=5, 有 tool_calls → 但 steps >= 5 → stop (强制停止)
```

**注释里的设计哲学**（routing.py:37-41）：

> 只用步数封顶,不在这里卡 token。防打转靠的是步数:一步一次模型调用,数得清、好解释，各家 Agent SDK 给的也是 max_turns / max_iterations 这类步数上限。
> token 花销是另一件事——它管的是烧钱和撑爆上下文,不是死循环。

---

### 机制 2：`tool_calls` 决定是否继续

**`should_continue` 的核心判断**：

```python
last = state["messages"][-1]
has_tool_calls = isinstance(last, AIMessage) and bool(last.tool_calls)
if not has_tool_calls:
    return "stop"
```

**AIMessage 的两种形态**：

#### 形态 A：有 tool_calls（继续循环）

```python
AIMessage(
    content="",
    tool_calls=[{"name": "query_logistics", "args": {...}}]
)
```

→ `has_tool_calls = True` → 返回 `"continue"` → 去执行工具

#### 形态 B：无 tool_calls（停止）

```python
AIMessage(
    content="您的快递在广州，预计明天送达",
    tool_calls=[]
)
```

→ `has_tool_calls = False` → 返回 `"stop"` → 结束循环

---

### 机制 3：messages 列表的累加

**每个节点都往 `messages` 追加**：

```
初始: [HumanMessage("我的快递到哪了？")]

main_agent 追加:
  [HumanMessage(...), AIMessage(tool_calls=[...])]

agent_tools 追加:
  [HumanMessage(...), AIMessage(...), ToolMessage("参数错误")]

main_agent 追加:
  [..., ToolMessage(...), AIMessage(content="请问订单号")]
```

**为什么这样设计？**

模型需要看到**完整的对话历史**：
- 用户说了什么
- 我（模型）调用了什么工具
- 工具返回了什么结果
- 我接下来该怎么办

**如果不累加会怎样？**

模型每次只看到用户问题，不知道自己刚调过工具 → 会重复调用同一个工具 → 死循环

---

## 五、验证场景：正常的工具调用

### 场景：用户说"查一下订单 1001 的物流"

**初始 state**：

```python
{
    "messages": [HumanMessage("查一下订单 1001 的物流")],
    "steps": 0,
    "user_id": "u123"
}
```

### 预测循环过程

#### 第 1 步：main_agent（第一次）

**模型推理**：用户提到了订单号 1001，我需要调用 `query_logistics`

**返回**：
```python
AIMessage(
    content="",
    tool_calls=[{"name": "query_logistics", "args": {"order_id": "1001"}}]
)
```

**state 变化**：`steps: 0 → 1`

---

#### 第 2 步：should_continue

**判断**：
- 有 `tool_calls` ✅
- `steps = 1 < max_agent_steps` ✅
- 返回 `"continue"`

---

#### 第 3 步：agent_tools

**执行**：调用 `query_logistics(order_id="1001")`

**工具返回**（成功）：
```python
ToolMessage(
    content="订单1001的快递在广州，预计明天送达",
    tool_call_id="call_abc123",
    name="query_logistics"
)
```

**state 变化**：messages 追加 ToolMessage

---

#### 第 4 步：main_agent（第二次）

**发给模型的内容**：
```
SystemMessage: "你是客服助手..."
HumanMessage: "查一下订单 1001 的物流"
AIMessage: tool_calls=[query_logistics(order_id="1001")]
ToolMessage: "订单1001的快递在广州，预计明天送达"  # ← 模型看到结果
```

**模型推理**：工具已经返回结果了，我可以直接告诉用户

**返回**：
```python
AIMessage(
    content="您的订单1001的快递在广州，预计明天送达",
    tool_calls=[]  # ← 没有工具调用
)
```

**state 变化**：`steps: 1 → 2`

---

#### 第 5 步：should_continue

**判断**：
- 无 `tool_calls` ❌
- 返回 `"stop"`

**结果**：跳到 `log` → END

---

### 完整流程对比表

| 步骤 | 节点 | steps | messages 长度 | 下一步 |
|---|---|---|---|---|
| 1 | main_agent | 0→1 | 2 (Human + AI) | continue |
| 2 | agent_tools | 1 | 3 (+ Tool) | 回 main_agent |
| 3 | main_agent | 1→2 | 4 (+ AI) | stop |
| 4 | log | 2 | 4 | END |

**关键数据**：
- 总共调用模型 **2 次**（main_agent 进入了 2 次）
- 最终 `steps = 2`
- messages 列表有 **4 条消息**

---

## 六、多工具调用场景

### 场景：用户说"查一下订单 1001 的物流和退款进度"

**问题**：模型可能会调用多个工具，怎么处理？

### 答案：agent_tools 批量执行

**代码逻辑**（nodes.py:361-400）：

```python
async def agent_tools(state) -> dict:
    tool_msgs = []
    for tc in last.tool_calls:  # ← 遍历所有工具调用
        run = await engine.execute_tool_call(tc, cid, specs, user_id=uid)
        tool_msgs.append(run.tool_message)
    
    return {"messages": tool_msgs}  # ← 一次性返回所有 ToolMessage
```

**执行过程**：

```
main_agent 返回：
  AIMessage(tool_calls=[
      {"name": "query_logistics", "args": {"order_id": "1001"}},
      {"name": "query_refund_status", "args": {"order_id": "1001"}}
  ])

agent_tools 执行：
  ① 调用 query_logistics → ToolMessage("快递在广州...")
  ② 调用 query_refund_status → ToolMessage("退款审核中...")
  
  返回：{"messages": [ToolMessage(...), ToolMessage(...)]}

main_agent 收到两条 ToolMessage，基于它们生成回复
```

**关键点**：
- `agent_tools` 一次性执行**所有**工具调用
- 返回**多条** ToolMessage
- 下一轮 `main_agent` 看到所有结果，综合回答

---

## 七、今天建立的核心认知

### ReAct 循环的本质

1. **main_agent**：调模型，决定"要不要调工具？调哪个？参数是什么？"
2. **agent_tools**：执行工具，返回结果
3. **should_continue**：判断"还要继续吗？"
4. 循环往复，直到模型决定不调工具（生成文本回复）

### 三个关键机制

| 机制 | 实现方式 | 作用 |
|---|---|---|
| **防死循环** | `steps` 字段 + 步数上限 | 强制停止，防止模型打转 |
| **循环条件** | `tool_calls` 是否为空 | 决定继续还是停止 |
| **上下文累加** | messages 列表追加 | 模型看到完整历史，不重复调用 |

### 能够预测的场景

✅ 给定用户输入 → 预测模型会调用哪些工具  
✅ 给定工具返回 → 预测循环会继续还是停止  
✅ 给定 steps 和 tool_calls → 画出完整循环过程  

---

## 八、待解决的问题

1. **工具系统的细节**：
   - `engine.execute_tool_call()` 内部做了什么？
   - 工具怎么注册的？（内置工具 + MCP 工具）
   - 参数校验在哪里做的？

2. **interrupt/resume 在 agent_tools 里的应用**：
   - `create_ticket` 的确认流程
   - `submit_refund` 的前端表单拦截
   - 为什么在 `agent_tools` 而不是工具内部？

3. **工具执行的安全机制**：
   - 订单归属校验（nodes.py:365-371, 397-403）
   - 写操作的确认流程
   - 审计日志

---

## 九、学习方法总结

### 今天的学习路径

1. **先看骨架**：两个节点 + 一个判断 + 一条回边
2. **走一遍完整循环**：从 steps=0 到 stop
3. **理解关键机制**：steps、tool_calls、messages 累加
4. **验证不同场景**：参数缺失、正常调用、多工具

### 读代码的技巧

**看循环代码要追踪的三个东西**：
1. **循环变量**：`steps`（什么时候加 1）
2. **循环条件**：`tool_calls`（什么时候为空）
3. **循环体**：`messages`（每次循环追加了什么）

### 下一步

深入**工具系统**：
- 工具注册表（registry）
- 执行引擎（engine）
- MCP 动态发现
- 参数校验和错误处理
