# MewHelp 学习笔记 —— 第二天：从困惑到框架感

> 日期：2026-09-14
> 仓库：https://github.com/fxfxfx123/mewhelp
> 目标：建立整体框架感，能预测路径、追踪字段、理解路径隔离

---

## 一、今天的学习目标调整

第一天抠了很多细节（`.get()` vs `[]`），但缺乏框架感——知道局部怎么工作，但不知道"我看的这段代码在系统什么位置"。

**今天重新定位**：不再抠语法细节，而是建立**整体框架感**。

**核心问题**：给定用户一句话，你能画出这句话会走过哪些节点、每个节点改了 state 的什么字段、最后怎么变成回复的。

---

## 二、整体框架：一句话的旅程

### 用户说："这个能退吗？"

系统会经历 **7 个节点**：

```
START
  ↓
① resolve_reference (指代消解：补全成"这个订单能退吗？")
  ↓
② classify_intent (意图识别：intent="退款退货")
  ↓ 判断：意图是"退款退货" → 走 refund_flow 路径
  ↓
③ fetch_order (抓订单号：从消息提取或让用户选择)
  ↓
④ retrieve_policy (检索退款政策)
  ↓
⑤ main_agent (调模型判断能不能退，基于订单数据+政策证据)
  ↓ 判断：没有 tool_calls → 停止
  ↓
⑥ log (记日志)
  ↓
END (返回给用户)
```

### State 白板的变化过程

| 节点名 | 这个节点做什么 | state 新增的字段 |
|---|---|---|
| **START** | 入口 | `messages`, `conversation_id`, `user_id` |
| ① **resolve_reference** | 补全指代 | `resolved_query` |
| ② **classify_intent** | 判意图 | `intent`, `route`, `intent_confidence` |
| ③ **fetch_order** | 抓订单号 | `order_id`, `order_data` |
| ④ **retrieve_policy** | 检索政策 | `evidence`, `citations` |
| ⑤ **main_agent** | 调模型生成回复 | `messages`（追加 AIMessage） |
| ⑥ **log** | 记日志 | `trace`（累加） |

**关键认知**：
- State 是**累加的白板**，每个节点只写自己负责的字段
- 下游节点可以读上游写的任何字段
- 不同路径写不同字段（这就是**路径隔离**）

---

## 三、5 条主路径全景图

系统有 5 条主要路径，从 `classify_intent` 分流：

```
START → resolve_reference → classify_intent
                                  ├─ "投诉" → complaint_reply → log → END
                                  ├─ "闲聊/其他" → script_reply → log → END
                                  ├─ "商品咨询" → retrieve_knowledge → confidence_check
                                  │                                       ├─ strong → main_agent ⇄ agent_tools
                                  │                                       └─ weak → fallback_reply → log → END
                                  ├─ "退款退货/售后" → fetch_order → retrieve_policy → main_agent ⇄ agent_tools
                                  └─ "物流/订单/人工" → main_agent ⇄ agent_tools
                                                            ↓
                                                          log → END
```

### 路径对比表

| 路径名 | 触发意图 | 节点数 | 调用模型次数 | 特点 |
|---|---|---|---|---|
| **投诉路** | "投诉" | 4 个 | 1 次 | 固定话术 + 转人工 |
| **闲聊路** | "闲聊"/"其他" | 4 个 | 1 次 | 最快，纯脚本回复 |
| **知识路（弱证据）** | "商品咨询" | 5 个 | 1 次 | 检索 → 兜底 |
| **知识路（强证据）** | "商品咨询" | 6+ 个 | 2+ 次 | 检索 → Agent 生成 |
| **退款路** | "退款退货" | 7+ 个 | 3+ 次 | 最长，有子流程 |
| **业务路** | "物流"/"订单" | 5+ 个 | 2+ 次 | ReAct 循环调工具 |

---

## 四、3 个关键分岔点（条件边）

### 分岔点 1：`route_by_intent`（classify_intent 之后）

**读取**：`state["intent"]`  
**判断**：查路由表 `INTENT_TO_ROUTE`

| 意图 | 返回值 | 下一个节点 |
|---|---|---|
| "投诉" | `"escalate"` | complaint_reply |
| "闲聊" | `"fallback_script"` | script_reply |
| "商品咨询" | `"knowledge"` | retrieve_knowledge |
| "退款退货" | `"refund_flow"` | fetch_order |
| "物流" | `"business"` | main_agent |

### 分岔点 2：`confidence_gate`（知识路专用）

**读取**：`state["evidence_strong"]`  
**判断**：证据强度够不够

| evidence_strong | 下一个节点 | 含义 |
|---|---|---|
| `True` | main_agent | 证据充分，让模型基于证据回答 |
| `False` | fallback_reply | 证据不足，返回兜底话术 |

### 分岔点 3：`should_continue`（ReAct 循环）

**读取**：`state["messages"][-1].tool_calls`  
**判断**：模型要不要继续调工具

| 条件 | 返回值 | 下一个节点 |
|---|---|---|
| 有 `tool_calls` 且 `steps < 上限` | `"continue"` | agent_tools |
| 无 `tool_calls` 或步数用完 | `"stop"` | log |

---

## 五、路径隔离：不同路径有不同的 state 形态

### 字段可见性表

| 字段名 | 闲聊路 | 知识路 | 退款路 | 业务路 |
|---|---|---|---|---|
| `messages` | ✅ | ✅ | ✅ | ✅ |
| `intent` | ✅ | ✅ | ✅ | ✅ |
| `route` | ✅ | ✅ | ✅ | ✅ |
| `resolved_query` | ✅ | ✅ | ✅ | ✅ |
| `evidence` | ❌ | ✅ | ✅ | ❌ |
| `evidence_strong` | ❌ | ✅ | ✅ | ❌ |
| `order_id` | ❌ | ❌ | ✅ | ❌ |
| `order_data` | ❌ | ❌ | ✅ | ❌ |
| `citations` | ❌ | ✅ | ✅ | ❌ |

### 核心规律

#### 规律 1：专用节点可以用 `[]` 直接读

**专用节点**：只在某条路径出现

例子：`retrieve_policy`（只在退款路）

```python
async def retrieve_policy(state) -> dict:
    od = state["order_data"]  # ← 安全！前面 fetch_order 保证写了
    seed = f"{base} {od.get('status', '')}".strip()
    # ...
```

**为什么安全？**
- `build.py:39`：`b.add_edge("fetch_order", "retrieve_policy")`
- `retrieve_policy` 的唯一入口是 `fetch_order`
- `fetch_order` 保证写入 `order_data`

#### 规律 2：通用节点必须用 `.get()` 防御

**通用节点**：多条路径都会经过

例子：`log_node`（所有路径的终点）

```python
async def log_node(state) -> dict:
    logger.info(
        "order=%s evidence=%s",
        state.get("order_id"),        # ← 只有退款路有
        "yes" if state.get("evidence") else "no"  # ← 只有知识路/退款路有
    )
```

**如果用 `state["order_id"]`**：
- 闲聊路 → KeyError 崩溃 💥
- 知识路 → KeyError 崩溃 💥
- 退款路 → 正常 ✅

#### 规律 3：`.get()` + if 的组合

```python
if state.get("evidence"):
    parts.append(state["evidence"])  # ← 进入 if 后，键一定存在
```

**推理链条**：
1. `state.get("evidence")` 有两种可能：
   - 键不存在 → 返回 `None` → falsy → 跳过 if
   - 键存在 → 返回值（可能是字符串、空字符串等）
2. 只有进入 if 内部，才说明键一定存在
3. 所以 if 内部可以安全地用 `[]`

---

## 六、真实案例：歧义场景的路径判断

### 场景："我的 iPhone 15 订单能退吗？"

这句话既像商品咨询（问产品能不能退），又像退款退货（我的订单要退）。

#### 判成"商品咨询" → 走知识路

```
classify_intent (intent="商品咨询")
  → retrieve_knowledge (检索政策)
  → confidence_check
  → main_agent (基于 evidence 回答)
```

**state 快照**：
```python
{
    "intent": "商品咨询",
    "evidence": "[1] 7天内可退...",  # ✅ 有
    "citations": [...],             # ✅ 有
    "order_id": ???,                # ❌ 没有
    "order_data": ???               # ❌ 没有
}
```

**回复**：通用政策解答，"根据退货政策[1]，7天内可以退..."

#### 判成"退款退货" → 走退款路

```
classify_intent (intent="退款退货")
  → fetch_order (提取订单号 / interrupt 弹选择器)
  → retrieve_policy (检索政策 + 订单状态)
  → main_agent (基于 evidence + order_data 判断)
```

**state 快照**：
```python
{
    "intent": "退款退货",
    "evidence": "[1] 7天内可退...",  # ✅ 有
    "citations": [...],             # ✅ 有
    "order_id": "1001",             # ✅ 有
    "order_data": {"status": "已发货", "created_at": "2024-09-06"}  # ✅ 有
}
```

**回复**：针对具体订单，"您的订单1001已发货3天，符合7天退货政策，可以申请退款"

#### 措辞影响路径

| 用户消息 | 大概率意图 | 走哪条路 | 有 order_id 吗 |
|---|---|---|---|
| "iPhone 15 能退吗？" | 商品咨询 | knowledge | ❌ |
| "我的订单能退吗？" | 退款退货 | refund_flow | ✅ |
| "我的 iPhone 15 订单能退吗？" | 退款退货 | refund_flow | ✅ |
| "你们的退货政策是什么？" | 商品咨询 | knowledge | ❌ |

**核心规律**：
- "我的" / "订单号" → 退款退货路（需要具体订单信息）
- 通用咨询词 → 商品咨询路（只需要政策知识）

---

## 七、今天建立的核心认知

### Level 1：路径预测 ✅
- 给定用户输入 → 预测意图 → 画出节点序列

### Level 2：字段追踪 ✅
- 知道每个字段在哪个节点写入
- 知道哪些节点能读到哪些字段
- 理解为什么有些节点必须用 `.get()`

### Level 3：路径隔离 ✅
- 理解不同路径有不同的 state 形态
- 知道为什么通用节点要防御性编程
- 能推理"如果改成 `[]` 会在哪条路径崩溃"

---

## 八、待解决的问题（下一步）

1. **ReAct 循环的执行机制**（第三天重点）：
   - `main_agent` 怎么决定调用哪个工具？
   - `agent_tools` 怎么执行工具？
   - 为什么需要 `should_continue` 判断？
   - 循环怎么停下来？

2. **interrupt/resume 的暂停恢复**：
   - `fetch_order` 里的 `interrupt()` 做了什么？
   - 用户点选订单后，怎么从断点继续？
   - state 怎么存到数据库的？

3. **检索和置信度闸的细节**：
   - `retrieve_knowledge` 的混合检索策略
   - `evidence_confidence` 怎么计算？
   - 为什么需要 `confidence_check` 节点？

---

## 九、学习方法论总结

### 从第一天的"细节优先"到今天的"框架优先"

**第一天**：抠 `.get()` vs `[]` 细节 → 见树不见林  
**第二天**：先建框架感（路径、分岔点、字段生命周期）→ 再往里填细节

**有效的学习顺序**：
1. 整体框架（5 条路径 + 3 个分岔点）
2. 一条完整路径的 state 变化
3. 字段生命周期追踪
4. 路径隔离的防御性编程
5. 细节语法（`.get()` 的使用时机）

### 读源码的三个问题

每读一个节点函数，只问三个问题：
1. 它往 state **写了**什么字段？
2. 它从 state **读了**什么字段？
3. 它什么时候被叫醒（哪条边连到它）？

### 验证方法

不要只看代码，要**推演场景**：
- 给定用户输入 → 画路径 → 追踪 state 变化
- 改变措辞 → 预测路径会不会变
- 假设改代码（`.get()` 改成 `[]`）→ 推理会在哪条路径崩溃

---

## 十、第二天成果

**起点**：知道局部细节，但缺乏框架感  
**终点**：能画出 5 条路径，能追踪字段生命周期，理解路径隔离

**最重要的一句话**：
> 不同路径有不同的 state 形态，专用节点可以安全用 `[]`，通用节点必须用 `.get()` 防御。理解这个，就理解了为什么 LangGraph 的代码要这样写。

**下一步**：深入 ReAct 循环 — 看 `main_agent` ⇄ `agent_tools` 这个循环怎么运转的。
