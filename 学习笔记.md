# MewHelp 学习笔记 —— 第一天：从困惑到框架

> 日期：2026-09-09
> 仓库：https://github.com/fxfxfx123/mewhelp
> 目标：看懂这个客服 Agent 的架构，能讲出来，能仿写一个

---

## 一、我今天的困惑（当时的真实卡点）

1. **模型是怎么按固定格式吐 JSON 的？** 直觉上模型只会吐文本，JSON 是谁教它的？
2. **JSON 在整个框架里怎么传？发生在哪个地方？** 总感觉哪里都是 JSON，但又抓不住。
3. **看不懂语法**：`return {"intent": r.intent, ...}` 这种写法是什么意思？为什么到处都是 `{...}`？
4. **工作流和脚本我懂，一沾上 LangChain 工程化就懵。** 感觉是另一套世界观。
5. **写 skill 时从没卡过**——Claude 读说明书、自己调工具、自己填参数。为什么换成代码实现，就到处都是东西要管？

## 二、核心突破：五个"啊哈"时刻

### 1. 模型吐 JSON 的真相：它在"调用一个假工具"

模型本身不知道什么叫 JSON。`with_structured_output(Pydantic类)` 干的事是：

- 把你定义的 Pydantic 类（如 `_Intent`）**伪装成一个工具**，塞进请求的 `tools` 字段
- 用 `tool_choice` 强制模型必须调它
- 模型以为自己在做 function calling，**它"调用工具的参数"就是你要的 JSON**
- JSON 藏在响应的 `tool_calls[].function.arguments` 里（是个字符串）
- LangChain 拿到后 `json.loads` → Pydantic 校验 → 变成 Python 对象

项目里只用 function calling 一条路（`app/core/llm.py` 的 `structured()`），因为实测 json_schema 和 json_mode 都会被中转层或模型搞坏。

### 2. JSON 只在"线上"存在，图内部全是 Python 对象

这是最大的顿悟。**JSON 是跨网络传输时的衣服，一进进程就脱掉：**

```
浏览器 ──JSON──> FastAPI ──拆成──> state dict(LangGraph 内部流转)
                                      │
                                      ├──拼成 JSON──> 大模型 API
                                      │<── JSON 响应 ──
                                      │
                              LangChain 当场解析成 Python 对象
                              合并回 state,继续用 dict 流转
```

LangGraph 节点之间传的**不是 JSON**，是内存里的 Python dict（state）。所以代码里的 `{...}` 全是 dict 字面量，不是 JSON。鉴别方法：被引号包着的才是 JSON 字符串，作为值直接传递的是 dict。

### 3. State 是一块共享白板

```python
async def classify_intent(state) -> dict:
    return {"intent": intent, "intent_confidence": conf}
```

- 每个节点函数收到全局 state，返回一个 dict
- LangGraph 把返回的 dict **合并**进 state（不是覆盖）
- 下游节点读 state 做判断（如路由 `route_by_intent` 读 `state["intent"]`）
- state 的字段清单在 `app/graph/state.py` 的 `ConversationState` 里

整个 LangGraph = 一块共享白板（state）+ 一群读写白板的函数（节点）+ 按白板内容决定走哪条边的路由器（条件边）。

### 4. LangChain 全是"换皮"，没有新东西

| 我已经会的 | LangChain 写法 | 本质 |
|---|---|---|
| 提示词模板 | `ChatPromptTemplate.from_messages([...])` | 带 `{变量}` 占位的字符串 |
| 提示词发给模型 | `prompt \| model` | 管道：前者输出喂后者 |
| 模型按格式吐 JSON | `model.with_structured_output(Pydantic类)` | 伪装成工具 |
| 一个步骤 | `def node(state) -> dict` | 普通函数，签名固定 |
| 步骤串联 | `add_edge("A", "B")` | A 干完干 B |
| if-else 分流 | `add_conditional_edges("A", fn, 映射表)` | 就是 if-else 画成图 |
| 循环调工具 | 边指回去：`add_edge("B", "A")` | ReAct 环 |
| 步骤间传数据 | 全局 state | 共享白板 |

只需要先记住 4 个 API：`StateGraph(State)`、`add_node`、`add_edge`、`add_conditional_edges`。

### 5. 工程化 = 把 Claude Code 脑子里隐式做的事显式化

这是我"为什么写 skill 从没卡过、看代码却懵"的答案：

**写 skill 时，Claude Code 是大脑。** 编排、判断、循环、纠错、记忆——全是它自动做的，我从没看见过。

**MewHelp 里没有 Claude。** 大脑的位置是空的，得用代码造一个"低配版 Claude"。以前黑盒里自动发生的事，全部摊在桌面上：

| Claude 自动做的（我从没见过） | MewHelp 里对应的代码 |
|---|---|
| 决定下一步干嘛 | `should_continue()` 条件边 |
| 记住刚聊了什么 | `state["messages"]` + checkpointer |
| 判断信息够不够 | `selfcheck.py` 证据闸 |
| 填工具参数 | 模型吐 `tool_calls` 的 JSON |
| 出错重试 | `llm.py` 的 `_run` 重试循环 |
| 循环调工具直到完成 | `main_agent ⇄ agent_tools` ReAct 环 |
| 停下来问"确认吗" | `interrupt` |

**每看到一段工程代码就问自己：如果是我+Claude Code 做这件事，这一步是 Claude 在哪一瞬间自动做的？**

### 6. 为什么要显式用代码写出来？（五个理由）

1. **可复制**：代码是判断力的无限分身，不需要你在线
2. **可靠**：业务红线焊死在代码里（投诉必走兜底、写操作必须人确认），模型不许越过
3. **可审查**：每个决策有留痕（`turn conv=42 intent=... route=...`），出事找得到出处
4. **可中断恢复**：`interrupt` 等用户确认，几小时后接着跑，状态在数据库里
5. **省钱**：确定性的事（闲聊回固定话术）写死代码，0 成本；模型只做非它不可的判断

---

## 三、系统全景（面试讲解版）

用户一句话进来，模型被叫醒 3~7 次，每次干一个小活：

```
用户:"这个还能退吗"
  │
  ▼
① resolve_reference   调模型→吐文本(指代消解,补全成完整问句)
  ▼
② classify_intent     调模型→吐 JSON {"intent":"退款退货","confidence":0.9}
  ▼
③ route_by_intent     纯 Python!查路由表 INTENT_TO_ROUTE → "refund_flow"
  ▼
④ fetch_order         纯 Python!抽订单号、查库、缺号则 interrupt 等用户选
  ▼
⑤ retrieve_policy     调模型→吐 JSON(Query 扩写)→ Python 拿 3 条 query 去检索
  ▼
⑥ main_agent          调模型→自主决定调工具(submit_refund)
  ▼
⑦ agent_tools         纯 Python!执行/拦截工具(退款拦成前端确认卡片)
  ▼
⑧ main_agent(第2轮)   调模型→吐文本(给用户的最终回答)
  ▼
log → END
```

**分工原则：确定性的逻辑焊死在代码里（路由、查库、校验、必须先检索、写操作人确认），模糊的判断留给模型（补全指代、判意图、判能不能退、措辞）。JSON 是两个世界之间的接口。**

---

## 四、Python 语法补丁（今天学的）

- `-> str` / `-> dict`：**返回值类型注解**。只是标签，Python 不强制执行，但 IDE 会检查，LangChain/LangGraph 靠它读信息
- `{"intent": x}`：dict 字面量，Python 内存对象，不是 JSON
- `f"...{x}..."`：f-string，字符串里嵌变量
- `async def` + `await`：调模型/查库要等待，所以要异步
- `state.get("order")` vs `state["order"]`：前者键不存在返回 None 不报错；后者会 KeyError（闲聊路没有 order 键，所以用 .get）
- `Annotated[list, add_messages]`：告诉 LangGraph 这个字段合并时追加而不是覆盖

---

## 五、明天到公司：源码阅读路线

仓库：https://github.com/fxfxfx123/mewhelp

**顺序（不要从头读！）：**

1. `README.md` —— 温习项目全貌和技术栈
2. `app/graph/build.py`（62 行）—— 整张工作流图，三样东西都在这：节点注册、边、条件边
3. `app/graph/state.py` —— 白板上有哪些字段
4. `app/graph/routing.py` —— 分流规则（`INTENT_TO_ROUTE` 表）
5. `app/graph/nodes.py` —— 挑两个节点细读：
   - `classify_intent`（简单的，调模型→写 state）
   - `main_agent` + `agent_tools`（ReAct 环，模型自主调工具）
6. `app/core/llm.py` 的 `structured()` —— 结构化输出的全部踩坑记录（注释比代码值钱）
7. `app/core/intent.py`（34 行）—— 全书最好的入门教材，逐行能读懂就算过关
8. `app/core/prompts.py` —— 提示词长什么样，改一改看效果

**每读一个文件只问三个问题：**
- 它往 state 写了什么字段？
- 它从 state 读了什么字段？
- 它什么时候被叫醒（哪条边连到它）？

---

## 六、后续学习计划（4 周）

- **第 1 周**：架构话术练熟（能对白板讲 5 分钟）+ 从零写一个 mini 客服 Agent 的 Step 1（纯对话，~50 行）
- **第 2-3 周**：mini 项目 Step 2（加工具调用 ReAct 循环，~100 行）、Step 3（加结构化输出+路由，~150 行）
- **第 4 周**：回 MewHelp 对号入座（我的 if-else ↔ 它的条件边）+ 模拟面试

**mini 项目是简历作品。面试话术：**"我自己从零写过一个客服 Agent，支持意图识别路由、工具调用循环、政策必须先检索再回答。复杂版可以参考 LangGraph 的条件边，好处是流程可视化、可中断恢复。"

---

## 七、留给明天的问题（还没解决的）

1. 上次翻译练习留的两道题还没答：
   - 如果退款路要先判断"订单是否超过7天"，超了直接拒答，在 LangGraph 版本里加在哪？
   - `answer_node` 里为什么用 `state.get("order")` 而不是 `state["order"]`？
2. `interrupt/resume` 具体怎么实现的？（退款流程等用户点确认）
3. checkpointer 是什么？state 怎么存数据库的？
4. RAG 检索的 `hybrid_rerank` 策略细节（ch04）
5. 有条件的话：把服务跑起来 `make dev`，开聊天页发一句话，`tail -f log/app.log` 看日志里每个节点逐条出现——脑子里的图会变成实时的

---

## 八、第一天总结（最重要的一页）

**起点状态**：Python 基础薄弱，能写工作流 skill 配合脚本（指挥 Claude Code），但一沾 LangChain 工程化就懵。

**一天下来真正发生了什么**：

我以为我在学"JSON 怎么传"，实际发现的是：**我从来没看见过 Agent 的幕后。** 写 skill 时，Claude 是大脑，编排、记忆、判断、循环全是它自动做的；MewHelp 是把这个大脑拆掉，用代码手工重建一台。懵不是因为难，是因为第一次被迫看见隐形的东西。

**今天建立的核心认知，三句话：**

1. **一次提问 = 多次模型调用**。模型是被反复叫醒的外包，每个节点叫它干一个小活；它要么吐文本（给用户），要么吐 JSON（给代码），JSON 就是两个世界的接口。

2. **图内流转的是 Python dict（state 白板），不是 JSON**。JSON 只在跨网络时存在（浏览器↔服务器、服务器↔模型），一进进程就被解析成对象。节点 return dict 合并进白板，条件边读白板做转向——这就是 LangGraph 的全部。

3. **工程化 = 把"本来 Claude 自动做的事"逐个显式化**。记忆、循环、重试、确认、兜底，每一个都变成一段看得见的代码。我缺的不是能力，是把已有直觉翻译成代码的映射表。

**我的优势**：我见过一个成熟 Agent 的 loop 长什么样（天天在用），业务拆解直觉比多数后端工程师好。

**我要补的**：翻译层——同一个流程，从"我脑子里的图"到"框架代码"的对应关系。练法：写 mini 项目，每写一步问自己"这一步本来是 Claude 自动做的哪件事"。

**方法论（以后一直用）**：
- 困惑随手记进这个文档，不马上解决也行，攒着
- 解决后回来补答案，这个文档就是我的私人教材
- 读源码只问三句话：写什么字段？读什么字段？什么时候被叫醒？
- 不要从头读代码，带着问题顺藤摸瓜
