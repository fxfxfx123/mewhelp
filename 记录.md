【可观测性】

为什么存在：
最终回答错了，仅看输出无法判断错误在哪一层。

作用：
记录一次请求经过各节点的输入、输出、路径、耗时、token、检索结果、tool call 等。

本质：
**让黑盒执行过程变成可诊断的 trace**。


【数据飞轮】

为什么存在：
真实用户会不断产生开发阶段没有覆盖的 failure。

流程：
bad case
→ 分类
→ 判断根因
→ 补知识 / 改检索 / 改 Agent / 改 Tool
→ 加入测试集
→ 回归验证
→ 再上线

本质：
让线上失败持续转化成新的开发任务。

1、json进、json出。本质就是通过系统提示词约束。